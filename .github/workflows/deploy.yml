# CI/CD Pipeline désactivé temporairement
# Pour réactiver, renommer ce fichier en deploy.yml

name: CI/CD Pipeline ChantirPro Next.js (DISABLED)

on:
  workflow_dispatch: # Seulement sur déclenchement manuel

jobs:
  test:
    runs-on: ubuntu-latest
    name: Tests & Validation
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm install
    
    - name: Run linter
      run: npm run lint || true
    
    - name: Build project
      run: npm run build